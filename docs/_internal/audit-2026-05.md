# Content audit — May 2026

Snapshot of every interior page, captured during the IA + content audit
pass that produced this PR. Each row records what was checked, what
was found, and what action (if any) was taken.

Audit methodology:
- Cross-checked every CLI claim against the running `data-product-forge==0.8.0` build's `fluid --help` and `fluid version` output.
- Cross-checked every YAML schema claim against `fluid_build/schemas/fluid-schema-0.7.2.json`.
- Cross-checked every PyPI package reference against `pypi.org/pypi/{name}/json`.
- Spot-checked external URLs by `curl -I`.

| Page | Length | Status | Issues found | Action |
|------|--------|--------|--------------|--------|
| `README.md` (root) | ~470 lines | rewrite-in-place | 44-cmd matrix listed dead commands; YAML had wrong schema (`dataQuality`, `governance: { accessPolicy }`, `format: duckdb_table`) | rewritten in PR 2 + PR 5 |
| `docs/README.md` (VuePress home) | ~300 lines | rewrite-in-place | Same YAML schema issues; `binding.resource` instead of `binding.format`+`binding.location` | fixed in PR 2 |
| `docs/getting-started/README.md` | 256 lines | fix-in-place | `pip install fluid-forge` (wrong PyPI name); `Fluid Forge CLI v0.7.7/8` (wrong version + format) | fixed in PR 2 |
| `docs/walkthrough/local.md` | 514 lines | fix-in-place | `fluid-forge` install, `v0.7.7` output | fixed in PR 2 |
| `docs/walkthrough/gcp.md` | 1355 lines | split-candidate | One of the longest pages in the repo. Single linkable URL drowns specific subsections. | NOT split in this PR — too risky to do under the same content-fix umbrella; tracked as `docs-content #split-gcp-walkthrough` |
| `docs/walkthrough/jenkins-cicd.md` | 773 lines | split-candidate | similar | tracked for follow-up |
| `docs/walkthrough/export-orchestration.md` | 702 lines | review | similar | tracked for follow-up |
| `docs/walkthrough/airflow-declarative.md` | 657 lines | review | similar | tracked for follow-up |
| `docs/cli/*.md` | 7 pages | partial | Repo claims "44 commands" but only 7 commands have dedicated pages: README, init, validate, plan, apply, verify, generate-airflow. The other ~22 commands have no pages. | CLI matrix in README rewritten; per-command pages NOT created in this PR (37 page-writing tasks) — tracked as `docs-content #cli-pages` |
| `docs/providers/*.md` | 8 pages | fix-in-place | `fluid-forge` deps in code samples; `(v0.7.1)` "current" stamps stale | fixed in PR 2 |
| `docs/advanced/*.md` | 7 pages | review | Mixed grab-bag: blueprints / governance / airflow / custom-llm-agents / forge-copilot-{discovery,memory} / chatgpt-forge-contract-gpt. No coherent grouping. | Reorganization deferred — sidebar restructure in this PR puts them under "Advanced" for now |
| `docs/vision.md` | 192 lines | fix-in-place | "v0.7.1 (current)" stamp; `fluidhq.io` footer; `fluid-forge` PyPI URL | fixed in PR 2 |
| `docs/contributing.md` | 108 lines | fix-in-place | `fluidhq.io` footer | fixed in PR 2 |
| `docs/RELEASE_NOTES_0.7.1.md` | 354 lines | keep-as-is | Historical content — leave alone but rename in sidebar | sidebar tweak only |
| `CODE_OF_CONDUCT.md`, `SECURITY.md` | both | fix-in-place | `security@fluidhq.io` (wrong domain) | fixed in PR 2: → `security@fluidforge.dev` |
| `docs/.vuepress/config.ts` | 158 lines | bug-fix | `repo: 'agentics-rising/fluid-forge-cli'` → 404'd every "Edit on GitHub" link. Same for `docsRepo` and the navbar GitHub link. | fixed in PR 2 |
| `docs/concepts/*` | none | **NEW** | Whole section missing — no central conceptual reference for Contract / Builds-Exposes-Bindings / Providers vs Platforms / Quality / Governance / agentPolicy / vs-alternatives | new in PR 5: 7 stub pages, each with verified schema-derived content + a "this is a stub" footer pointing at `docs-content` issue tag |
| `docs/recipes/*` | none | **NEW** | No solution-oriented recipes. | new in PR 5: 1 index + 1 starter recipe (add-a-quality-rule); 7 more queued in the index for community contributions |
| `docs/faq/*` | none | **NEW** | No FAQ; comparison-vs-alternatives never had a home page. | new in PR 5: starter Q&A with 7 entries |

## Critical fixes shipped in PR 2 (Phase 1 content sweep)

| Class of fix | Count | Files |
|--------------|-------|-------|
| `fluid-forge` PyPI name → `data-product-forge` | ~30 references | docs/, README.md, package.json, badges |
| `fluidhq.io` URLs → `https://github.com/Agentics-Rising` | 10 in `forge_docs`, 5 in `forge-cli` | footers, package.json author, AGENTS.md |
| `security@fluidhq.io` → `security@fluidforge.dev` | 4 references | CODE_OF_CONDUCT.md, SECURITY.md (both repos) |
| `Fluid Forge CLI v0.7.X` (stale, wrong format) → `FLUID CLI v0.8.0` | 3 instances | getting-started, walkthrough/local, cli/README |
| `fluidVersion: "0.7.1"` → `"0.7.2"` (current schema) | 11 files | walkthroughs, providers, cli, vision |
| `(v0.7.1)` "current" stamps → reflect actual current | 2 files | vision.md, providers/README.md |
| YAML examples with invalid schema (`dataQuality`, `governance: {...}`, `format: duckdb_table`) | 2 files | README.md, docs/README.md |
| Broken `repo`/`docsRepo` config in vuepress | 1 file | docs/.vuepress/config.ts |
| Outdated 8-category command matrix → real v0.8.0 7-group taxonomy | 2 files | README.md (forge_docs + forge-cli) |
| Missing CLI commands (`bundle`, `rollback`, `verify-signature`, `import`, `split`, `config`, `schedule-sync`, `generate <subcmd>`) added; renamed `marketplace` → `market`; removed dead commands (`execute`, `contract-tests`, `viz-graph`, `viz-plan`, `preview`, `wizard`, etc.) | 2 files | README.md (forge_docs + forge-cli) |

## Out of scope — explicit follow-up tickets

- `docs-content #split-gcp-walkthrough` — split `walkthrough/gcp.md` (1355 lines) into 5 pages
- `docs-content #cli-pages` — write 22+ missing CLI command pages
- `docs-content #full-faq` — replace FAQ stubs with real Q&A as questions arrive in Discussions
- `docs-content #recipes` — fill the 7 reserved recipe slots
- `docs-content #concepts-{contract,builds,providers,quality,governance,agent,vs}` — replace each concept stub with the long-form treatment
- `docs-content #mobile-perf` — critical-CSS extraction to push mobile Lighthouse from ~70 → ≥95

Audit run on production build (`docs/.vuepress/dist/`) — desktop Lighthouse confirmed ≥95 perf/a11y/bp/seo on home / getting-started / cli / walkthrough/local; playground sits at 87 perf (Monaco bundle, expected).
